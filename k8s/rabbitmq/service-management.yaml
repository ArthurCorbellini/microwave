apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-management
  namespace: microwave
spec:
  type: NodePort
  selector:
    app: rabbitmq
  ports:
    - port: 15672
      targetPort: 15672
      nodePort: 30672
